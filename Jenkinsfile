pipeline {
    agent any  

    stages {

        stage('Init') {
            steps {
                echo 'Init'
                echo '******************************'
            }
        }

        stage('Yarn Install') {
            steps {
                echo 'Yarn Install'
                echo '******************************'
            }
        }

        stage('Yarn Build') {
            steps {
                echo 'Yarn Build'
                echo '******************************'
            }
        }

        // Add additional stages or comments if required.

        stage('Deploy') {
            steps {
                echo 'Deploy'
                echo '******************************'
            }
        }
    }
}
